import { Injectable } from '@nestjs/common'; import { HealthStatus } from '@prisma/client'; import { PrismaService } from '@/common/prisma/prisma.service'; @Injectable() export class DashboardService{constructor(private prisma:PrismaService){} async executive(){const [activeProjects,delayedMilestones,pendingApprovals,criticalActivities,openNcrs,materialShortages,tenders]=await Promise.all([this.prisma.project.count({where:{status:'ACTIVE'}}),this.prisma.planningMilestone.count({where:{healthStatus:{in:[HealthStatus.DELAYED,HealthStatus.CRITICAL]}}}),this.prisma.tender.count({where:{stage:'APPROVAL_PENDING'}}),this.prisma.planningActivity.count({where:{isCritical:true}}),this.prisma.ncr.count(),this.prisma.planningResource.count({where:{healthStatus:{in:[HealthStatus.AT_RISK,HealthStatus.DELAYED,HealthStatus.CRITICAL]}}}),this.prisma.tender.count()]); const projects=await this.prisma.project.findMany({take:6,orderBy:{createdAt:'desc'},include:{projectManager:{select:{name:true}}}});return {kpis:{activeProjects,portfolioCompletion:61,delayedMilestones,pendingApprovals,criticalActivities,openNcrs,materialShortages,tenders},projects}} async planningPortfolio(){const projects=await this.prisma.project.findMany({include:{activities:true,milestones:true,resources:true},orderBy:{createdAt:'desc'}});return projects.map(p=>({id:p.id,code:p.code,name:p.name,clientName:p.clientName,healthStatus:p.healthStatus,plannedProgress:p.plannedProgress,actualProgress:p.actualProgress,scheduleVariance:p.actualProgress-p.plannedProgress,totalActivities:p.activities.length,criticalActivities:p.activities.filter(a=>a.isCritical).length,delayedMilestones:p.milestones.filter(m=>['DELAYED','CRITICAL'].includes(m.healthStatus)).length,resourceShortages:p.resources.filter(r=>['AT_RISK','DELAYED','CRITICAL'].includes(r.healthStatus)).length}))}}
