import { Controller, Get, Param, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { JwtAuthGuard } from '@/common/guards/jwt-auth.guard'; import { PlanningService } from './planning.service'; @ApiTags('Project Planning') @ApiBearerAuth() @UseGuards(JwtAuthGuard) @Controller('api/v1/project-plans') export class PlanningController{constructor(private service:PlanningService){} @Get(':projectId/summary') summary(@Param('projectId') projectId:string){return this.service.summary(projectId)} @Get(':projectId/activities') activities(@Param('projectId') projectId:string){return this.service.activities(projectId)} @Get(':projectId/milestones') milestones(@Param('projectId') projectId:string){return this.service.milestones(projectId)} @Get(':projectId/resources') resources(@Param('projectId') projectId:string){return this.service.resources(projectId)} @Get(':projectId/critical-path') criticalPath(@Param('projectId') projectId:string){return this.service.criticalPath(projectId)} @Get(':projectId/drilldown') drilldown(@Param('projectId') projectId:string){return this.service.drilldown(projectId)}}
